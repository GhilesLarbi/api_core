/////////////////////////////////////////////////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////////////////////////////////
export type FileUpload = {
  isPending: boolean
  mutate: (
    file: File,
    handlers: { onSuccess: (url: string) => void; onError: () => void }
  ) => void
}
